name: providerhive
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  # using this for local storage database
  hive_flutter: ^1.1.0
  # Using this for checking path of file in flutter
  path_provider: ^2.0.11
  provider: ^6.0.3
  built_value_generator: ^8.4.3
  http: ^0.13.5
  uuid: ^3.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  build_runner: ^2.3.3
  hive_generator: ^1.1.3

flutter:
  uses-material-design: true